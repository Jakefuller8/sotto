{
  "name": "sotto-relay",
  "version": "1.2.0",
  "description": "Encrypted relay that carries dictated text from a phone to a laptop.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node test.js"
  },
  "engines": { "node": ">=22" }
}
